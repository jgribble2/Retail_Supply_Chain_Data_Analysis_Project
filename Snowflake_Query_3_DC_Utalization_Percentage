SELECT
  DC_ID,
  REGION,
  STORAGE_CAPACITY,
  CURRENT_UTILIZATION,
  (CURRENT_UTILIZATION::float / STORAGE_CAPACITY) * 100 AS UTILIZATION_PERCENTAGE
FROM
    "RETAIL_SUPPLY_CHAIN_DATA_PROJECT"."PUBLIC"."DISTRIBUTION_CENTERS" AS DC;
